import { useEffect,useState } from 'react';
import { api } from './api';
type Invoice={id:number|string;invoice_number:string;customer?:{first_name:string;last_name:string};status:string;invoice_date:string;due_date:string;total_amount:string|number;amount_paid:string|number;amount_due:string|number;currency:string};
export default function InvoicesPage(){const [items,setItems]=useState<Invoice[]>([]),[search,setSearch]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState('');async function load(term=search){setLoading(true);setError('');try{const q=term?`?search=${encodeURIComponent(term)}`:'';const r=await api<{data:Invoice[]}>('/invoices'+q);setItems(r.data??[])}catch(e){setError(e instanceof Error?e.message:'Unable to load invoices.')}finally{setLoading(false)}}useEffect(()=>{load('')},[]);return <section className="content"><div className="welcome"><div><small>ACCOUNTS RECEIVABLE</small><h2>Invoices</h2><p>Track customer charges, balances and due dates.</p></div></div>{error&&<p className="error">{error}</p>}<div className="panel"><div className="toolbar"><input value={search} onChange={e=>setSearch(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()} placeholder="Search invoice number or customer"/><button onClick={()=>load()}>Search</button></div>{loading?<p>Loading invoices…</p>:items.length===0?<p>No invoices found.</p>:<div className="tableWrap"><table><thead><tr><th>Invoice</th><th>Customer</th><th>Issued</th><th>Due</th><th>Total</th><th>Paid</th><th>Due</th><th>Status</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{i.invoice_number}</td><td>{i.customer?`${i.customer.first_name} ${i.customer.last_name}`:'—'}</td><td>{i.invoice_date}</td><td>{i.due_date}</td><td>{i.currency} {i.total_amount}</td><td>{i.currency} {i.amount_paid}</td><td>{i.currency} {i.amount_due}</td><td><span className="statusBadge">{i.status}</span></td></tr>)}</tbody></table></div>}</div></section>}
